// User view of Statement page — view only (no import button shown because user?.role !== 'admin')
// The same component handles both roles via isAdmin check.
export { default } from '@/pages/admin/budget/StatementPage';
